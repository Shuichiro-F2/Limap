-- 投稿画像に、グリッド/カードなど小さい表示専用の軽量サムネイル画像への
-- パスを別途持たせる。投稿数の多いアカウントのプロフィール画面などで、
-- 一覧表示のたびにフルサイズ画像を大量に同時デコードして動作が重くなる
-- (再読み込みが繰り返される)問題を解消するための変更。
--
-- 既存行はnullのままで問題ない(アプリ側はthumbnail_pathがnullの場合、
-- 従来通りフル画像のURLにフォールバックして表示する)。
alter table public.spot_images add column thumbnail_path text;
